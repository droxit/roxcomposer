# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      # use `-browsers` prefix for selenium tests, e.g. `3.6.1-browsers`
      - image: circleci/python:3.6.1

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "requirements.txt" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run:
          name: install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt

      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}

      # run tests!
      - run:
          name: run tests
          command: |
            . venv/bin/activate
            python start.py test

      # build package
      - run:
          name: build package
          command: |
            . venv/bin/activate
            mkdir ./test-reports;
            tar cz --exclude venv -f ./test-reports/build.tar.gz *;
      # create pip package
      - run:
          name: create pip package
          command: |
            . venv/bin/activate
            python setup.py sdist
            cp ./dist/*.tar.gz ./test-reports/
      - store_artifacts:
          path: test-reports
          destination: test-reports
      - deploy:
          name: deploy to gru
          command: |
            . venv/bin/activate
            scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ./test-reports/mosaic*.tar.gz devpi@176.9.144.37:/tmp
            ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null devpi@176.9.144.37 'devpi login droxit --password=droxit; devpi use http://localhost:4040/droxit/dev; devpi upload /tmp/mosaic*.tar.gz; rm /tmp/mosaic*.tar.gz; devpi logoff'
#          dev:
#            branch: dev
#            commands: |
#              . venv/bin/activate
#              scp ./test-reports/pip_package.tar.gz 176.9.144.37:/tmp
#              ssh devpi@176.9.144.37 'devpi login droxit --password=droxit; devpi upload --index droxit/dev /tmp/pip_package.tar.gz; devpi logoff'
